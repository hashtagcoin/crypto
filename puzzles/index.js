'use strict'

const hashing = require('../hashing')
const signing = require('../signing')

class Puzzle {
  static create(claims = {}) {

  }
  static verify(puzzle = {}, solutions = []) {

  }
}

module.exports = Puzzle
